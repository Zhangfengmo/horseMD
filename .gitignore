node_modules/
out/
dist/
*.log
.DS_Store

# mobile (Capacitor) build output + native build artifacts
dist-mobile/
ios/App/Pods/
ios/App/App/public/
ios/App/build/
ios/DerivedData/
android/.gradle/
android/app/build/
android/build/
android/local.properties
android/app/src/main/assets/public/
.capacitor/

# Android release signing — NEVER commit these (lose the keystore = can't update the app)
android/key.properties
*.keystore
*.jks

# local test fixtures (generated)
test-articles/

# local agent/browser runtime state
.omc/
.playwright-mcp/

# scratch/preview images and design references
build/_*.png
*参考*.html

# local video production workspace (video-shotcraft): captures, audio,
# Remotion dependencies, renders, and QA frames are intentionally local-only
.horsemd-video/
.agents/
skills-lock.json

# device build (xcodebuild -derivedDataPath)
ios/App/build-device/
.vercel

# IDE
.idea/
