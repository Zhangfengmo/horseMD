---
title: 文件与保存
description: 排查 HorseMD 文档找不到、未保存、工作区刷新和移动端文件位置问题。
---

# 文件与保存

<span class="version-badge">适用于 HorseMD v0.13.51</span>

## 工作区会复制文件吗

不会。桌面端添加文件夹只是把真实磁盘目录挂到文件树，重命名、移动和删除都会作用于真实文件。移除工作区根目录只取消显示，不删除目录。

## 未命名草稿在哪里

有内容且未保存的临时标签会随会话恢复，并保持“已修改”。它存放在 HorseMD 用户数据中，不是已经创建的 `.md` 文件。重要内容应使用 `Ctrl/Cmd+S` 保存到明确位置。

## 外部程序改了文件

HorseMD 监听已打开文件和工作区目录。磁盘变化会刷新文件树；当前标签没有未保存内容时会自动同步新内容。若你正在编辑，应用不会直接用外部版本覆盖，而是弹出提醒：本地未保存内容已保留；此时保存会覆盖外部版本，如需保留两份请使用“另存为”。

## 富文本保存会重新排版整篇源码吗

不会。HorseMD 会保留一份作者原始 Markdown，只把富文本中实际修改的区域写回去。没有触碰的列表符号、空行、CRLF/BOM、紧凑或松散列表不会因为一次普通编辑被整篇格式化；编辑器内部用于空段落的 `<br />` 也不会写入源码。

列表类型转换、标题转换等结构操作会修改被操作的当前块，这是用户明确触发的变化；其父级、子级和其他段落仍应保持原样。需要逐字符控制复杂反斜杠、LaTeX 或特殊 Markdown 结构时，建议直接使用源码模式。

如果应用提示“无法安全映射到源码”并暂停保存，说明它选择保留当前富文本而不是冒险覆盖文件。可以同意用当前富文本重建源码；若严格重建仍失败或不希望规范化原文，选择新路径保存 `.horsemd-recovered.md` 恢复副本。恢复副本不会覆盖原文件，但可能规范化空行、列表 marker 和转义。表格中的内部空 cell 占位会按保存时仍可证明的结构坐标清除，作者手写的 `<br />` 保持不变；坐标身份仍可证明的恢复表格应能重新打开、继续编辑并正常保存，行列移动等无法证明的结构变化仍会安全暂停。请保留原文件与恢复副本，并附上复现步骤反馈。正常的段落、列表、引用、反引号、表格和模式切换路径已经纳入逐字输入、保存与完整重开回归。

退出应用时出现“有未保存文件”的确认，只决定是否继续关闭，不代表同意重建或规范化文件源码。在确认完成前，有路径文件只读取已经通过验证的富文本快照，不会运行 strict rebuild、推进源码基线或写入未验证内容；恢复内容只有在用户显式选择另存路径时才会写入新文件。

## Android 文件管理器看不到文档

Android 可能禁止普通文件管理器浏览应用目录。请在 HorseMD 文件树中打开，或使用“导出/分享”复制到公共目录。卸载前必须导出重要文档。

## 图片或附件移动后失效

相对路径以 Markdown 文件目录为基准。移动文档时应同时移动关联的图片文件夹和 `assets/`。在源码模式检查链接是否仍指向正确相对位置。

::: tip 反馈时提供路径类型
报告文件问题时说明是绝对路径、相对路径、`file:///` 链接、网络地址，还是移动端系统 URI；不要公开包含敏感信息的完整个人路径。
:::
